class Solution:
    def repeatedCharacter(self, s: str) -> str:
        seen=set()#create an empty set
        for ch in s:
            if ch in seen: 
                return ch
            seen.add(ch) # it will add all non repeat word
